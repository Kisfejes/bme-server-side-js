<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Songbook</a>
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= currentPath.includes('/session') ? 'active' : '' %>">
        <a class="nav-link" href="/sessions">
          Sessions
          <span class="sr-only">(current)</span>
        </a>
      </li>
      <%if (currentUser.role === 'ADMIN') { %>
        <li class="nav-item <%= currentPath.includes('/song') ? 'active' : '' %>">
          <a class="nav-link" href="/songs">
            Songs
            <span class="sr-only">(current)</span>
          </a>
        </li>
      <% } %>

    </ul>
    <div class="form-inline my-2 my-lg-0">
      <span><%= currentUser.name %></span>
      <a href="/logout">
        <img class="sign-out-icon" src="/assets/icons/sign-out.svg" height="24" width="24" />
      </a>
    </div>
  </div>
</nav>
